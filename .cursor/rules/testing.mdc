---
description: Testing conventions
globs: ["src/**/*.test.*", "src/**/*.spec.*", "tests/**"]
alwaysApply: false
---
- No .only() or .skip() in committed tests.
- Minimum coverage 80%.
- Integration tests with a real DB instance.
- Names: describe("what") → it("does what when condition").
